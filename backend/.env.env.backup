# Database Configuration
# Render PostgreSQL Database (Singapore)
DATABASE_URL=postgresql://recommender_qhkw_user:k8cvRIaH5rSns9saC2EEhkBQnAvzGfV6@dpg-d58jdfre5dus73dvifd0-a.singapore-postgres.render.com/recommender_qhkw

# JWT Settings
JWT_SECRET_KEY=your-secret-key-change-in-production
JWT_ALGORITHM=HS256

# CORS Settings (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001

# Redis (optional - leave empty if not using)
REDIS_URL=

# Qdrant (optional - leave empty if not using)
QDRANT_URL=http://localhost:6333

# Server Configuration
HOST=127.0.0.1
PORT=8000
ENVIRONMENT=development
